<div class = "container">
  <h1>All Lists</h1>

  <% @lists.each do |list|  %>
    <ul>
    <li><%= link_to list.name, list_path(list) %></li>
    </ul>
  <% end %>

<%= link_to "Create a list", new_list_path(@list) %>
